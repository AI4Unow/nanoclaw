# Upgrade-safe overlay for NanoClaw agent container.
# Routes API calls through api.ai4u.now and forces a proxy-supported
# model alias so Claude Code does not default to unsupported canonical
# Anthropic model IDs.
#
# Rebuild after NanoClaw updates:
#   docker build -t nanoclaw-agent:latest container/
#   docker build -t nanoclaw-agent-custom:latest -f Dockerfile.custom .

FROM nanoclaw-agent:latest
ENV ANTHROPIC_BASE_URL=https://api.ai4u.now
ENV ANTHROPIC_MODEL=sonnet-4.6
